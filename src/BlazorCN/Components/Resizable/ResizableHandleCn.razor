@namespace BlazorCN
@inherits ComponentBaseCn

<div class="@Cn.Merge(DefaultClasses, Class)"
     style="@Style"
     data-slot="resizable-handle"
     tabindex="0"
     role="separator"
     aria-label="Resize"
     aria-orientation="@AriaOrientation"
     aria-valuemin="0"
     aria-valuemax="100"
     @attributes="AdditionalAttributes">
    @if (WithHandle)
    {
        <div class="z-10 flex h-6 w-1 shrink-0 rounded-lg bg-border @(IsVertical ? "rotate-90" : "")"></div>
    }
</div>

@code {
    [CascadingParameter] public ResizablePanelGroupCn? PanelGroup { get; set; }
    [Parameter] public bool WithHandle { get; set; }

    private bool IsVertical => PanelGroup?.Direction == ResizableDirection.Vertical;

    // aria-orientation refers to the SEPARATOR axis. In a vertical group (panels stacked top/bottom),
    // the separator is HORIZONTAL. In a horizontal group, the separator is VERTICAL.
    private string AriaOrientation => IsVertical ? "horizontal" : "vertical";

    // pointer-coarse:after:* widens the invisible ::after hit zone to ~16px on touch
    // devices (react-resizable-panels uses 15px hitAreaMargins); the visible 1px bar
    // is unchanged and the existing translate keeps the zone centered.
    private string DefaultClasses => IsVertical
        ? "relative flex h-px w-full items-center justify-center bg-border ring-offset-background cursor-row-resize touch-none select-none after:absolute after:left-0 after:h-1 after:w-full after:-translate-y-1/2 pointer-coarse:after:h-4 focus-visible:outline-hidden focus-visible:ring-1 focus-visible:ring-ring"
        : "relative flex w-px items-center justify-center bg-border ring-offset-background cursor-col-resize touch-none select-none after:absolute after:inset-y-0 after:left-1/2 after:w-1 after:-translate-x-1/2 pointer-coarse:after:w-4 focus-visible:outline-hidden focus-visible:ring-1 focus-visible:ring-ring";
}
