@namespace BlazorCN
@inherits ComponentBaseCn

<textarea class="@CssClass"
          style="@Style"
          placeholder="@Placeholder"
          disabled="@Disabled"
          data-slot="input-group-control"
          @oninput="HandleInput"
          @attributes="AdditionalAttributes">@Value</textarea>

@code {
    [Parameter] public string? Value { get; set; }
    [Parameter] public EventCallback<string> ValueChanged { get; set; }
    [Parameter] public string? Placeholder { get; set; }
    [Parameter] public bool Disabled { get; set; }

    private static readonly string BaseClasses =
        "cn-input-group-textarea flex flex-1 field-sizing-content min-h-16 w-full resize-none outline-none disabled:cursor-not-allowed disabled:opacity-50";

    private string CssClass => Cn.Merge(BaseClasses, Class);

    private async Task HandleInput(ChangeEventArgs e)
    {
        await ValueChanged.InvokeAsync(e.Value?.ToString());
    }
}
