@namespace BlazorCN
@inherits ComponentBaseCn

<div class="@Cn.Merge("relative mt-1.5 h-[var(--radix-navigation-menu-viewport-height)] w-full overflow-hidden rounded-md border bg-popover text-popover-foreground shadow-md", Class)" style="@Style" data-slot="navigation-menu-viewport" @attributes="AdditionalAttributes">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
