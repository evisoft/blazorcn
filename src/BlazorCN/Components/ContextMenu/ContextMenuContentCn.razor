@namespace BlazorCN
@inherits ComponentBaseCn
@implements IAsyncDisposable

@if (ContextMenu?.IsOpen == true)
{
    <div @ref="_contentRef"
         class="@Cn.Merge("cn-context-menu-content z-50 max-h-(--available-height) origin-(--transform-origin) overflow-x-hidden overflow-y-auto", Class)"
         style="@($"position:fixed;left:{ContextMenu.ClientX}px;top:{ContextMenu.ClientY}px;{Style}")"
         data-slot="context-menu-content"
         data-state="open"
         @attributes="AdditionalAttributes">
        @ChildContent
    </div>
}
