@namespace BlazorCN
@inherits ComponentBaseCn

<div class="@Cn.Merge("cn-context-menu-label", Class)" style="@Style" data-slot="context-menu-label" data-inset="@(Inset ? "" : null)" @attributes="AdditionalAttributes">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool Inset { get; set; }
}
