@namespace BlazorCN
@inherits ComponentBaseCn
@implements IAsyncDisposable

@if (Sub?.IsOpen == true)
{
    <div @ref="_contentRef"
         class="@Cn.Merge("cn-context-menu-sub-content z-50 max-h-(--available-height) origin-(--transform-origin) overflow-x-hidden overflow-y-auto", Class)"
         style="@Style"
         role="menu"
         aria-orientation="vertical"
         data-slot="context-menu-sub-content"
         data-state="open"
         data-side="@Side.ToString().ToLowerInvariant()"
         data-align="@Align.ToString().ToLowerInvariant()"
         @attributes="AdditionalAttributes">
        @ChildContent
    </div>
}
