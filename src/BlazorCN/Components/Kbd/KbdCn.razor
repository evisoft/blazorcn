@namespace BlazorCN
@inherits ComponentBaseCn

<kbd class="@Cn.Merge("pointer-events-none inline-flex h-5 items-center gap-1 rounded border bg-muted px-1.5 font-mono text-[10px] font-medium text-muted-foreground select-none", Class)"
     style="@Style"
     data-slot="kbd"
     @attributes="AdditionalAttributes">
    @ChildContent
</kbd>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
