@namespace BlazorCN
@inherits ComponentBaseCn

<span class="@Cn.Merge("flex size-full items-center justify-center rounded-full bg-muted", Class)"
      style="@Style"
      data-slot="avatar-fallback"
      @attributes="AdditionalAttributes">
    @ChildContent
</span>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
