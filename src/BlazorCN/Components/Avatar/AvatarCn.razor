@namespace BlazorCN
@inherits ComponentBaseCn

<span class="@Cn.Merge("relative flex size-10 shrink-0 overflow-hidden rounded-full", Class)"
      style="@Style"
      data-slot="avatar"
      @attributes="AdditionalAttributes">
    @ChildContent
</span>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
