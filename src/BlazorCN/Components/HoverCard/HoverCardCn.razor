@namespace BlazorCN
@inherits ComponentBaseCn
@implements IDisposable

<div class="@Cn.Merge("relative inline-block", Class)" style="@Style" data-slot="hover-card" data-state="@(IsOpen ? "open" : "closed")" @attributes="AdditionalAttributes">
    <CascadingValue Value="this" IsFixed="false">
        @ChildContent
    </CascadingValue>
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public int OpenDelay { get; set; } = 200;
    [Parameter] public int CloseDelay { get; set; } = 100;

    internal bool IsOpen { get; private set; }
    internal ElementReference TriggerElement { get; set; }

    private CancellationTokenSource? _openCts;
    private CancellationTokenSource? _closeCts;

    internal async Task RequestOpen()
    {
        // Cancel any pending close
        _closeCts?.Cancel();
        _closeCts = null;

        if (IsOpen) return;

        _openCts?.Cancel();
        _openCts = new CancellationTokenSource();
        var token = _openCts.Token;

        try
        {
            await Task.Delay(OpenDelay, token);
            if (!token.IsCancellationRequested)
            {
                IsOpen = true;
                StateHasChanged();
            }
        }
        catch (TaskCanceledException) { }
    }

    internal async Task RequestClose()
    {
        // Cancel any pending open
        _openCts?.Cancel();
        _openCts = null;

        if (!IsOpen) return;

        _closeCts?.Cancel();
        _closeCts = new CancellationTokenSource();
        var token = _closeCts.Token;

        try
        {
            await Task.Delay(CloseDelay, token);
            if (!token.IsCancellationRequested)
            {
                IsOpen = false;
                StateHasChanged();
            }
        }
        catch (TaskCanceledException) { }
    }

    internal void CancelClose()
    {
        _closeCts?.Cancel();
        _closeCts = null;
    }

    public void Dispose()
    {
        _openCts?.Cancel();
        _openCts?.Dispose();
        _closeCts?.Cancel();
        _closeCts?.Dispose();
    }
}
