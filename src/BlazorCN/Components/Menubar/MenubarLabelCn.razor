@namespace BlazorCN
@inherits ComponentBaseCn

<div class="@Cn.Merge("cn-menubar-label", Class)" style="@Style" data-slot="menubar-label" data-inset="@(Inset ? "" : null)" @attributes="AdditionalAttributes">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool Inset { get; set; }
}
