@namespace BlazorCN
@inherits ComponentBaseCn

<div class="@Cn.Merge("cn-dropdown-menu-label", Class)" style="@Style" data-slot="dropdown-menu-label" data-inset="@(Inset ? "" : null)" @attributes="AdditionalAttributes">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool Inset { get; set; }
}
