@* ============================================================
   Block: billing-feature-gating-preview
   Title: BillingFeatureGatingPreview
   Description: A billing feature gating and locked feature preview block for React and Next.js with visual locks and upgrade prompts built with shadcn/ui and Tailwind CSS
   Source: original/blocks/billing/billing-feature-gating-preview.tsx
   ============================================================ *@

<section class="mx-auto w-full max-w-2xl p-4">
    <div class="relative overflow-hidden rounded-lg border bg-card">
        <div class="border-b px-4 py-3 flex items-center justify-between">
            <div class="flex items-center gap-2">
                <LucideIconCn Name="bar-chart-3" Size="16" Class="size-4 text-muted-foreground" />
                <span class="font-medium text-sm">Advanced Insights</span>
            </div>
            <BadgeCn Variant="BadgeVariant.Secondary" Class="h-5 gap-1 text-[10px] font-semibold uppercase">
                <LucideIconCn Name="zap" Size="10" Class="size-2.5 fill-amber-500 text-amber-500" />
                Pro Feature
            </BadgeCn>
        </div>

        <div class="relative p-6">
            <div class="grid grid-cols-2 gap-4 opacity-20 grayscale pointer-events-none select-none">
                <div class="h-24 rounded-md bg-muted/50 border border-dashed"></div>
                <div class="h-24 rounded-md bg-muted/50 border border-dashed"></div>
                <div class="h-32 col-span-2 rounded-md bg-muted/50 border border-dashed"></div>
            </div>

            <div class="absolute inset-0 z-10 flex flex-col items-center justify-center bg-background/40 backdrop-blur-[2px] p-6 text-center">
                <div class="flex size-12 items-center justify-center rounded-full bg-primary shadow-lg text-primary-foreground">
                    <LucideIconCn Name="lock" Size="20" Class="size-5" />
                </div>

                <h3 class="mt-4 font-semibold text-sm">Unlock Advanced Analytics</h3>
                <p class="mt-2 max-w-[280px] text-muted-foreground text-xs leading-relaxed">
                    Get deeper insights into your team's performance with custom reports, real-time
                    tracking, and automated exports.
                </p>

                <div class="mt-6 flex items-center gap-3">
                    <ButtonCn Size="ButtonSize.Sm" Class="h-8 gap-2 shadow-sm">
                        <LucideIconCn Name="sparkles" Size="14" Class="size-3.5" />
                        Upgrade to Pro
                    </ButtonCn>
                    <ButtonCn Variant="ButtonVariant.Ghost" Size="ButtonSize.Sm" Class="h-8 text-xs">
                        Learn More
                    </ButtonCn>
                </div>
            </div>
        </div>

        <div class="border-t bg-muted/30 px-4 py-2.5">
            <p class="text-center text-muted-foreground text-[10px]">
                Join 2,000+ teams who have already upgraded.
            </p>
        </div>
    </div>
</section>
