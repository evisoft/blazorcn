@* ============================================================
   Block: login-compact-inline
   Title: Login Compact Inline
   Description: Ultra-compact single-row React inline login form for Next.js with email and password inputs and submit button in one horizontal line using shadcn/ui Input, Button, Tailwind CSS, and framer-motion expand animation
   Long Description: Clean minimal vertical login form
   Dependencies: ButtonCn, InputCn, LucideLogInCn
   Source: original/blocks/login/login-compact-inline.tsx
   ============================================================ *@

<section class="mx-auto w-full max-w-2xl p-4">
    <div class="overflow-hidden rounded-lg border bg-card">
        <div class="flex items-center gap-2 p-3">
            <LucideLogInCn Size="16" Class="size-4 shrink-0 text-muted-foreground" />
            <InputCn Type="email" Placeholder="Email address" Class="h-8 min-w-0 flex-1 text-sm" />
            <InputCn Type="password" Placeholder="Password" Class="h-8 w-[140px] text-sm" />
            <ButtonCn Size="ButtonSize.Sm" Class="h-8 shrink-0 px-4 text-xs">
                Sign in
            </ButtonCn>
        </div>
    </div>
</section>

@code {
}
