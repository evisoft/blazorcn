@* ============================================================
   Block: sidebar-favorites-section
   Title: SidebarFavoritesSection
   Description: Organized navigation with React, Next.js, shadcn/ui Sidebar, and Tailwind CSS featuring starred favorites group and quick-add action button for instant page access.
   Source: original/blocks/sidebar/sidebar-favorites-section.tsx
   ============================================================ *@

<section class="mx-auto w-full max-w-6xl p-4">
    <div class="overflow-hidden rounded-lg border bg-card">
        <div class="flex h-[700px]">
            @* Sidebar *@
            <div class="flex w-64 shrink-0 flex-col border-r bg-sidebar">
                @* Sidebar Header *@
                <div class="flex h-16 items-center border-b px-2">
                    <a href="#" class="flex w-full items-center gap-2 rounded-md p-2 hover:bg-sidebar-accent hover:text-sidebar-accent-foreground">
                        <div class="flex aspect-square size-8 items-center justify-center rounded-lg bg-sidebar-primary text-sidebar-primary-foreground">
                            <LucideIconCn Name="gallery-vertical-end" Size="16" Class="size-4" />
                        </div>
                        <div class="flex flex-col gap-0.5 leading-none">
                            <span class="font-medium">Workspace</span>
                            <span class="text-xs text-muted-foreground">Acme Inc.</span>
                        </div>
                    </a>
                </div>

                @* Sidebar Content *@
                <div class="flex flex-1 flex-col overflow-y-auto py-2">

                    @* Favorites Group *@
                    <div class="px-2">
                        @* Group label row with action button *@
                        <div class="flex items-center justify-between px-2 py-1">
                            <span class="flex items-center text-xs font-medium text-sidebar-foreground/60">
                                <LucideIconCn Name="star" Size="12" Class="mr-1 size-3" />
                                Favorites
                            </span>
                            <button type="button"
                                    title="Add to favorites"
                                    class="flex size-5 items-center justify-center rounded-md text-sidebar-foreground/60 hover:bg-sidebar-accent hover:text-sidebar-accent-foreground">
                                <LucideIconCn Name="plus" Size="16" Class="size-4" />
                            </button>
                        </div>
                        <ul class="mt-0.5 space-y-0.5">
                            @foreach (var itm in _favorites)
                            {
                                var item = itm;
                                <li>
                                    <a href="@item.Url"
                                       class="@Cn.Merge("flex items-center gap-2 rounded-md px-2 py-1.5 text-sm hover:bg-sidebar-accent hover:text-sidebar-accent-foreground", item.IsActive ? "bg-sidebar-accent text-sidebar-accent-foreground font-medium" : "")">
                                        <LucideIconCn Name="@item.Icon" Size="16" Class="size-4" />
                                        <span>@item.Title</span>
                                    </a>
                                </li>
                            }
                        </ul>
                    </div>

                    @* All Pages Group *@
                    <div class="mt-2 px-2">
                        <div class="px-2 py-1">
                            <span class="text-xs font-medium text-sidebar-foreground/60">All Pages</span>
                        </div>
                        <ul class="mt-0.5 space-y-0.5">
                            @foreach (var itm in _allPages)
                            {
                                var item = itm;
                                <li>
                                    <a href="@item.Url"
                                       class="flex items-center gap-2 rounded-md px-2 py-1.5 text-sm hover:bg-sidebar-accent hover:text-sidebar-accent-foreground">
                                        <LucideIconCn Name="@item.Icon" Size="16" Class="size-4" />
                                        <span>@item.Title</span>
                                    </a>
                                </li>
                            }
                        </ul>
                    </div>
                </div>

                @* Sidebar Rail *@
                <div class="absolute inset-y-0 right-0 w-px bg-border opacity-0 hover:opacity-100 cursor-col-resize"></div>
            </div>

            @* Main Content (SidebarInset) *@
            <div class="flex min-w-0 flex-1 flex-col">
                @* Header *@
                <header class="flex h-16 shrink-0 items-center gap-2 border-b px-4">
                    <button type="button" class="-ml-1 flex size-8 items-center justify-center rounded-md hover:bg-accent hover:text-accent-foreground">
                        <LucideIconCn Name="panel-left" Size="16" Class="size-4" />
                        <span class="sr-only">Toggle Sidebar</span>
                    </button>
                    <div class="mr-2 h-4 w-px bg-border"></div>
                    @* Breadcrumb *@
                    <nav aria-label="breadcrumb">
                        <ol class="flex flex-wrap items-center gap-1.5 break-words text-sm text-muted-foreground sm:gap-2.5">
                            <li class="hidden items-center md:flex">
                                <a href="#" class="transition-colors hover:text-foreground">Favorites</a>
                            </li>
                            <li class="hidden md:flex [&>svg]:size-3.5 [&>svg]:text-muted-foreground/70">
                                <LucideIconCn Name="chevron-right" Size="14" Class="size-3.5 text-muted-foreground/70" />
                            </li>
                            <li class="flex items-center">
                                <span role="link" aria-disabled="true" aria-current="page" class="font-normal text-foreground">Dashboard</span>
                            </li>
                        </ol>
                    </nav>
                </header>

                @* Body *@
                <div class="flex flex-1 flex-col gap-4 p-4">
                    <div class="grid auto-rows-min gap-4 md:grid-cols-3">
                        <div class="aspect-video rounded-xl bg-muted/50"></div>
                        <div class="aspect-video rounded-xl bg-muted/50"></div>
                        <div class="aspect-video rounded-xl bg-muted/50"></div>
                    </div>
                    <div class="flex-1 rounded-xl bg-muted/50"></div>
                </div>
            </div>
        </div>
    </div>
</section>

@code {
    private record NavItem(string Title, string Url, string Icon, bool IsActive = false);

    private static readonly NavItem[] _favorites =
    [
        new("Dashboard", "#", "layout-dashboard", true),
        new("Analytics", "#", "bar-chart"),
        new("Team Members", "#", "users"),
    ];

    private static readonly NavItem[] _allPages =
    [
        new("Inbox", "#", "inbox"),
        new("Calendar", "#", "calendar"),
        new("Documents", "#", "file-text"),
        new("Knowledge Base", "#", "book-open"),
        new("Projects", "#", "folder"),
        new("Messages", "#", "message-square"),
        new("Notifications", "#", "bell"),
        new("Settings", "#", "settings"),
    ];
}
