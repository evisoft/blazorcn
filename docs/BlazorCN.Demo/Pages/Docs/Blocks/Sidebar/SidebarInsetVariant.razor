@* ============================================================
   Block: sidebar-inset-variant
   Title: SidebarInsetVariant
   Description: Modern React sidebar with inset variant, rounded content panel, and shadow effects using Next.js, shadcn/ui Sidebar components, and Tailwind CSS styling.
   Source: original/blocks/sidebar/sidebar-inset-variant.tsx
   ============================================================ *@

<section class="mx-auto w-full max-w-6xl p-4">
    <div class="overflow-hidden rounded-lg border bg-card">
        <div class="flex h-[700px] w-full bg-sidebar/50">

            @* Inset Sidebar — sits flush with the outer border, content area appears inset/rounded *@
            <div class="flex w-64 shrink-0 flex-col bg-sidebar">

                @* Sidebar Header *@
                <div class="flex h-16 items-center px-4 py-2">
                    <a href="#" class="flex items-center gap-3 rounded-md px-2 py-2 text-sm font-medium hover:bg-sidebar-accent hover:text-sidebar-accent-foreground w-full">
                        <div class="flex aspect-square size-8 items-center justify-center rounded-lg bg-sidebar-primary text-sidebar-primary-foreground">
                            <LucideIconCn Name="gallery-vertical-end" Size="16" Class="size-4" />
                        </div>
                        <div class="flex flex-col gap-0.5 leading-none">
                            <span class="font-medium">Acme Project</span>
                            <span class="">Issue Tracker</span>
                        </div>
                    </a>
                </div>

                @* Sidebar Content *@
                <div class="flex-1 overflow-y-auto px-2 py-2">
                    @foreach (var grp in _navigation)
                    {
                        <div class="mb-2">
                            <div class="flex items-center px-2 py-1.5 text-xs font-medium text-sidebar-foreground/70">
                                @grp.Title
                            </div>
                            <ul class="space-y-0.5">
                                @foreach (var navItem in grp.Items)
                                {
                                    var isActive = navItem.IsActive;
                                    <li>
                                        <a href="@navItem.Url"
                                           class="@Cn.Merge("flex items-center gap-2 rounded-md px-2 py-1.5 text-sm text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground", isActive ? "bg-sidebar-accent font-medium text-sidebar-accent-foreground" : "")">
                                            <LucideIconCn Name="@navItem.Icon" Size="16" Class="size-4" />
                                            <span>@navItem.Title</span>
                                        </a>
                                    </li>
                                }
                            </ul>
                        </div>
                    }
                </div>

                @* Sidebar Rail (visual divider) *@
                <div class="absolute inset-y-0 right-0 hidden w-px translate-x-px bg-sidebar-border group-data-[side=left]/sidebar-wrapper:block"></div>

            </div>

            @* Sidebar Inset / Main Content *@
            <div class="flex min-w-0 flex-1 flex-col">
                <header class="flex h-16 shrink-0 items-center gap-2 border-b px-4">
                    @* Sidebar trigger button *@
                    <button type="button" class="-ml-1 flex size-7 items-center justify-center rounded-md text-muted-foreground hover:bg-accent hover:text-accent-foreground">
                        <LucideIconCn Name="panel-left" Size="16" Class="size-4" />
                        <span class="sr-only">Toggle Sidebar</span>
                    </button>
                    <div class="mr-2 h-4 w-px bg-border"></div>
                    @* Breadcrumb *@
                    <nav aria-label="breadcrumb">
                        <ol class="flex flex-wrap items-center gap-1.5 break-words text-sm text-muted-foreground sm:gap-2.5">
                            <li class="hidden items-center md:flex">
                                <a href="#" class="transition-colors hover:text-foreground">Issues</a>
                            </li>
                            <li class="hidden md:flex" aria-hidden="true">
                                <LucideIconCn Name="chevron-right" Size="14" Class="size-3.5" />
                            </li>
                            <li class="flex items-center">
                                <span role="link" aria-current="page" aria-disabled="true" class="font-normal text-foreground">All Issues</span>
                            </li>
                        </ol>
                    </nav>
                </header>
                <div class="flex flex-1 flex-col gap-4 p-4">
                    <div class="grid auto-rows-min gap-4 md:grid-cols-3">
                        <div class="aspect-video rounded-xl bg-muted/50"></div>
                        <div class="aspect-video rounded-xl bg-muted/50"></div>
                        <div class="aspect-video rounded-xl bg-muted/50"></div>
                    </div>
                    <div class="flex-1 rounded-xl bg-muted/50"></div>
                </div>
            </div>

        </div>
    </div>
</section>

@code {
    private record NavSubItem(string Title, string Url, string Icon, bool IsActive = false);
    private record NavGroup(string Title, NavSubItem[] Items);

    private static readonly NavGroup[] _navigation =
    [
        new("Issues", new NavSubItem[]
        {
            new("All Issues", "#", "circle-dot", true),
            new("My Issues", "#", "layout-list"),
            new("Assigned to Me", "#", "bookmark"),
            new("Recently Viewed", "#", "clock"),
        }),
        new("Development", new NavSubItem[]
        {
            new("Branches", "#", "git-branch"),
            new("Pull Requests", "#", "git-pull-request"),
            new("Releases", "#", "tag"),
        }),
    ];
}
