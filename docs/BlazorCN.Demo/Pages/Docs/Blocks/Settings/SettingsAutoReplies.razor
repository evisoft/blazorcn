@* ===========================================================
   Block: settings-auto-replies
   Title: Settings Auto Replies
   Description: React animated auto-reply settings block for Next.js with message templates, schedule configuration, trigger conditions, and preview toggle using shadcn/ui, framer-motion, and Tailwind CSS
   Long Description: Alert conditions and notification channels
   Dependencies: BadgeCn, ButtonCn, SwitchCn
   Source: original/blocks/settings/settings-auto-replies.tsx
   =========================================================== *@

    <section class="mx-auto w-full max-w-2xl p-4">
      <div class="overflow-hidden rounded-lg border bg-card">
        @* Header *@
        <div class="border-b px-4 py-3">
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-3">
              <LucideMailCn Class="h-5 w-5 text-muted-foreground" />
              <div>
                <h2 class="font-medium text-sm">Auto-Reply Rules</h2>
                <p class="text-muted-foreground text-xs">
                  Configure automated responses for incoming messages
                </p>
              </div>
            </div>
            <ButtonCn Size="ButtonSize.Sm" Class="h-7 gap-1.5 text-xs">
              <LucidePlusCn Class="h-3.5 w-3.5" />
              New Rule
            </ButtonCn>
          </div>
        </div>

        @* Rules list *@
        <div>
</div>
</div>
</section>

@code {
    private string _expanded = "ar_2";
    private string _ruleStates = "Object.fromEntries(rules.map(r => [r.id, r.enabled]";
}

