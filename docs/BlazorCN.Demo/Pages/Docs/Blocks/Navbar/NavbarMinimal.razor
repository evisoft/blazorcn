@* ============================================================
   Block: navbar-minimal
   Title: NavbarMinimal
   Description: A clean minimal navigation bar built with React, Next.js, shadcn/ui, and Tailwind CSS. Features a logo, horizontal nav links with hover underline animations, and a single call-to-action button using framer-motion transitions.
   Source: original/blocks/navbar/navbar-minimal.tsx
   ============================================================ *@

<section class="mx-auto w-full max-w-4xl p-4">
    <div class="overflow-hidden rounded-lg border bg-card">
        <nav class="flex items-center justify-between px-6 py-3">
            @* Logo *@
            <span class="font-medium text-sm">Acme Inc</span>

            @* Nav links *@
            <div class="flex items-center gap-1">
                @foreach (var link in _navLinks)
                {
                    var lbl = link.Label;
                    var isHovered = _hoveredLink == lbl;
                    <a href="@link.Href"
                       class="relative px-3 py-1.5 text-muted-foreground text-sm transition-colors hover:text-foreground"
                       @onmouseenter="@(() => _hoveredLink = lbl)"
                       @onmouseleave="@(() => _hoveredLink = null)"
                       @onclick:preventDefault>
                        @lbl
                        @if (isHovered)
                        {
                            <span class="absolute inset-x-3 -bottom-0.5 h-px bg-foreground"></span>
                        }
                    </a>
                }
            </div>

            @* CTA *@
            <ButtonCn Size="ButtonSize.Sm" Class="h-7 text-xs">
                Get Started
            </ButtonCn>
        </nav>
    </div>
</section>

@code {
    private record NavLink(string Label, string Href);

    private static readonly NavLink[] _navLinks =
    [
        new("Products", "#products"),
        new("Solutions", "#solutions"),
        new("Pricing", "#pricing"),
        new("Docs", "#docs"),
    ];

    private string? _hoveredLink = null;
}
