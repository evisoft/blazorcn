@* ============================================================
   Block: pricing-feature-unlock
   Title: PricingFeatureUnlock
   Description: A three-column pricing section for React and Next.js with plan cards organizing features into named categories like Core Platform, Team Tools, Security and Compliance, and Support, animated NumberFlow prices that respond to a PillTabs billing toggle, ShadcnioButton CTAs, and emerald checkmarks built with shadcn/ui Tooltip components and Tailwind CSS
   Source: original/blocks/pricing/pricing-feature-unlock.tsx
   ============================================================ *@

<section class="mx-auto w-full max-w-6xl p-4">
    @* Header *@
    <div class="text-center">
        <p class="text-xs font-medium uppercase tracking-widest text-muted-foreground">
            Pricing
        </p>
        <h2 class="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">
            Everything your team needs
        </h2>
        <p class="mx-auto mt-3 max-w-lg text-base text-muted-foreground leading-relaxed">
            Choose a plan that scales with you. Every feature organized so you can find exactly what
            matters.
        </p>

        @* Billing toggle *@
        <div class="mt-6 flex items-center justify-center">
            <div class="flex items-center rounded-full bg-muted p-1 text-sm font-medium">
                @foreach (var opt in _billingOptions)
                {
                    var option = opt;
                    <button type="button"
                            class="@Cn.Merge("rounded-full px-4 py-1.5 transition-colors", _billing == option ? "bg-background shadow-sm text-foreground" : "text-muted-foreground hover:text-foreground")"
                            @onclick="@(() => _billing = option)">
                        @(option == "monthly" ? "Monthly" : "Annually")
                    </button>
                }
            </div>
        </div>
    </div>

    @* Plan grid *@
    <TooltipProviderCn>
        <div class="mt-10 grid grid-cols-1 gap-4 md:grid-cols-3">
            @foreach (var plan in _plans)
            {
                var displayPrice = _billing == "monthly" ? plan.MonthlyPrice : plan.AnnualPrice;
                <article class="@Cn.Merge("relative flex h-full flex-col rounded-2xl bg-muted p-6", plan.IsPopular ? "ring-2 ring-foreground/10" : "")">
                    @* Reserved badge slot — rendered on every card so headings align *@
                    <div class="mb-4 flex h-7 items-start">
                        @if (plan.IsPopular)
                        {
                            <span class="inline-flex items-center rounded-full bg-red-500 px-3 py-1 text-[11px] font-extrabold uppercase tracking-wider text-white shadow-sm">
                                Best Value
                            </span>
                        }
                    </div>

                    @* Plan header *@
                    <div>
                        <h3 class="text-2xl font-bold tracking-tight">@plan.Name</h3>
                        <p class="mt-1.5 min-h-[2.75rem] text-sm text-muted-foreground leading-relaxed">
                            @plan.Description
                        </p>
                    </div>

                    @* Price — fixed height row for layout stability *@
                    <div class="mt-6 flex h-12 items-baseline gap-1.5 sm:h-14">
                        <span class="text-5xl font-bold tabular-nums tracking-tight">$@displayPrice</span>
                        <span class="text-sm text-muted-foreground">/mo</span>
                    </div>

                    @* CTA *@
                    <div class="mt-5">
                        <ButtonCn Class="w-full">@plan.Cta</ButtonCn>
                    </div>

                    @* Feature heading *@
                    <p class="mt-7 text-sm font-semibold">@plan.FeatureHeading</p>

                    @* Categorized features *@
                    <div class="mt-4 flex-1 space-y-5">
                        @foreach (var cat in plan.FeatureCategories)
                        {
                            <div>
                                <p class="text-[11px] font-semibold uppercase tracking-widest text-muted-foreground">
                                    @cat.Name
                                </p>
                                <ul class="mt-2.5 space-y-2.5">
                                    @foreach (var feat in cat.Features)
                                    {
                                        <li class="flex items-start justify-between gap-2">
                                            <span class="flex items-start gap-2.5">
                                                <LucideIconCn Name="check" Size="16" Class="mt-0.5 size-4 shrink-0 stroke-[3] text-emerald-600 dark:text-emerald-500" aria-hidden="true" />
                                                <span class="text-sm font-medium leading-snug text-foreground">
                                                    @feat.Name
                                                </span>
                                            </span>
                                            @if (!string.IsNullOrEmpty(feat.Tooltip))
                                            {
                                                <TooltipCn OpenDelay="200">
                                                    <TooltipTriggerCn>
                                                        <button type="button"
                                                                aria-label="@($"More info about {feat.Name}")"
                                                                class="mt-0.5 cursor-help">
                                                            <LucideIconCn Name="info" Size="12" Class="size-3 shrink-0 text-muted-foreground/40 transition-colors hover:text-muted-foreground" />
                                                        </button>
                                                    </TooltipTriggerCn>
                                                    <TooltipContentCn Side="FloatingSide.Top" Class="max-w-[220px]">
                                                        <p class="text-xs">@feat.Tooltip</p>
                                                    </TooltipContentCn>
                                                </TooltipCn>
                                            }
                                        </li>
                                    }
                                </ul>
                            </div>
                        }
                    </div>
                </article>
            }
        </div>
    </TooltipProviderCn>

    @* Footer *@
    <p class="mt-8 text-center text-xs text-muted-foreground">
        All paid plans include a 14-day free trial · No credit card required · Cancel anytime
    </p>
</section>

@code {
    private string _billing = "monthly";

    private static readonly string[] _billingOptions = ["monthly", "annually"];

    private record PlanFeature(string Name, string? Tooltip = null);
    private record PlanFeatureCategory(string Id, string Name, PlanFeature[] Features);
    private record PlanItem(string Id, string Name, string Description, int MonthlyPrice, int AnnualPrice, string FeatureHeading, PlanFeatureCategory[] FeatureCategories, string Cta, bool IsPopular);

    private static readonly PlanItem[] _plans =
    [
        new(
            "starter",
            "Starter",
            "For individuals and small projects getting off the ground.",
            19,
            15,
            "What's included:",
            [
                new("core", "Core Platform",
                [
                    new("Up to 10 projects"),
                    new("5GB storage with CDN"),
                    new("1,000 API requests/mo"),
                    new("Basic analytics", "Page views, unique visitors, and top referrers with 7-day retention."),
                ]),
                new("team", "Team Tools",
                [
                    new("Single-user workspace"),
                    new("Email notifications"),
                ]),
                new("support", "Support",
                [
                    new("Community forum"),
                    new("Knowledge base"),
                ]),
            ],
            "Get started",
            false
        ),
        new(
            "professional",
            "Professional",
            "For growing teams that need collaboration and advanced features.",
            49,
            39,
            "Everything in Starter, plus:",
            [
                new("core", "Core Platform",
                [
                    new("Unlimited projects"),
                    new("50GB storage + global CDN", "Content served from 40+ edge locations for sub-100ms latency worldwide."),
                    new("100K API requests/mo"),
                    new("Advanced analytics + funnels", "Conversion funnels, cohort analysis, and custom event tracking with 1-year retention."),
                ]),
                new("team", "Team Tools",
                [
                    new("Up to 10 team members"),
                    new("Real-time collaboration", "Live cursors, simultaneous editing, and inline commenting on any page."),
                    new("Slack & Teams integration"),
                ]),
                new("security", "Security & Compliance",
                [
                    new("Two-factor authentication"),
                    new("30-day version history"),
                ]),
                new("support", "Support",
                [
                    new("Priority email (8h SLA)"),
                ]),
            ],
            "Start 14-day trial",
            true
        ),
        new(
            "business",
            "Business",
            "For organizations needing enterprise security and scale.",
            99,
            79,
            "Everything in Professional, plus:",
            [
                new("core", "Core Platform",
                [
                    new("Unlimited storage"),
                    new("Unlimited API requests"),
                    new("Multi-region deployment", "Choose data storage in US, EU, or APAC to meet data residency requirements."),
                ]),
                new("team", "Team Tools",
                [
                    new("Unlimited team members"),
                    new("Custom workflows", "Build automated workflows with our visual editor."),
                ]),
                new("security", "Security & Compliance",
                [
                    new("SAML SSO + directory sync", "Connect Okta, Azure AD, or Google Workspace with SCIM auto-provisioning."),
                    new("Custom RBAC + audit logs"),
                    new("SOC 2 Type II + ISO 27001"),
                    new("HIPAA BAA available"),
                ]),
                new("support", "Support",
                [
                    new("24/7 phone & chat (4h SLA)"),
                    new("Dedicated account manager"),
                    new("99.95% uptime SLA"),
                ]),
            ],
            "Start 14-day trial",
            false
        ),
    ];
}
