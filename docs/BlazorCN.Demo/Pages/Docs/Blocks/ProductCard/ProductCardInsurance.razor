@* ========================================
   Block: product-card-insurance
   Title: Product Card Insurance
   Description: Insurance plan card with coverage details, deductible options, and premium calculator for insurance sites
   Dependencies: BadgeCn, ButtonCn, CardCn, CardContentCn, CardFooterCn, LabelCn, LucideCheckCn, LucideHelpCircleCn, LucideShieldCn, LucideXCn, SelectCn, SelectContentCn, SelectItemCn, SelectTriggerCn, SelectValueCn
   Source: original/blocks/product-card/product-card-insurance.tsx
   ======================================== *@

<section class="container mx-auto px-4 py-6">
    <div class="mb-6 text-center">
        <h1 class="text-3xl font-bold tracking-tight">Product Card</h1>
    </div>

    <div class="mx-auto w-[320px] max-w-full">
        <CardCn Class="overflow-hidden">
            <div class="flex items-center justify-between border-b p-4">
                <div class="flex items-center gap-3">
                    <div class="flex h-10 w-10 items-center justify-center rounded-full border">
                        <LucideShieldCn Class="h-5 w-5 text-primary" Size="20" />
                    </div>
                    <div>
                        <h3 class="font-semibold">@_product.Name</h3>
                        <BadgeCn Class="@_tierColor">@_product.Tier</BadgeCn>
                    </div>
                </div>
            </div>

            <CardContentCn Class="p-4">
                <p class="text-sm text-muted-foreground">@_product.Description</p>

                <div class="mt-4">
                    <LabelCn Class="text-xs font-medium text-muted-foreground">ANNUAL DEDUCTIBLE</LabelCn>
                    <SelectCn @bind-Value="_selectedDeductible">
                        <SelectTriggerCn Class="mt-1.5">
                            <SelectValueCn />
                        </SelectTriggerCn>
                        <SelectContentCn>
                            @foreach (var option in _product.Deductibles)
                            {
                                <SelectItemCn Value="@option.Amount.ToString()">$@option.Amount.ToString("N0") deductible</SelectItemCn>
                            }
                        </SelectContentCn>
                    </SelectCn>
                </div>

                <div class="mt-4">
                    <div class="mb-2 flex items-center justify-between">
                        <LabelCn Class="text-xs font-medium text-muted-foreground">COVERAGE</LabelCn>
                        <span class="text-xs text-muted-foreground">Max: @_product.MaxCoverage</span>
                    </div>
                    <div class="space-y-2">
                        @foreach (var coverage in _product.Coverages)
                        {
                            <div class="flex items-center justify-between text-sm">
                                <div class="flex items-center gap-2">
                                    @if (coverage.Included)
                                    {
                                        <LucideCheckCn Class="h-4 w-4 text-primary" Size="16" />
                                    }
                                    else
                                    {
                                        <LucideXCn Class="h-4 w-4 text-muted-foreground" Size="16" />
                                    }
                                    <span class="@(coverage.Included ? "" : "text-muted-foreground")">@coverage.Name</span>
                                </div>
                                @if (!string.IsNullOrEmpty(coverage.Limit) && coverage.Included)
                                {
                                    <span class="text-xs text-muted-foreground">@coverage.Limit</span>
                                }
                            </div>
                        }
                    </div>
                </div>

                <div class="mt-4 rounded-lg border p-3">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-xs text-muted-foreground">Estimated Monthly Premium</p>
                            <p class="text-2xl font-bold tabular-nums">$@_currentOption.Premium</p>
                        </div>
                        <ButtonCn Class="h-8 w-8" Size="ButtonSize.Icon" Variant="ButtonVariant.Ghost">
                            <LucideHelpCircleCn Class="h-4 w-4" Size="16" />
                        </ButtonCn>
                    </div>
                    <p class="mt-1 text-xs text-muted-foreground">With $@_currentOption.Amount.ToString("N0") deductible</p>
                </div>
            </CardContentCn>

            <CardFooterCn Class="flex-col gap-2 p-4 pt-0">
                <ButtonCn Class="w-full">Get Quote</ButtonCn>
                <ButtonCn Class="w-full" Variant="ButtonVariant.Outline">Compare Plans</ButtonCn>
            </CardFooterCn>
        </CardCn>
    </div>
</section>

@code {
    private string _selectedDeductible = "1000";

    private DeductibleOption _currentOption => _product.Deductibles.FirstOrDefault(d => d.Amount.ToString() == _selectedDeductible) ?? _product.Deductibles[1];

    private string _tierColor => _product.Tier switch
    {
        "Bronze" => "bg-orange-100 text-orange-800 dark:bg-orange-900 dark:text-orange-200",
        "Silver" => "bg-slate-100 text-slate-800 dark:bg-slate-800 dark:text-slate-200",
        "Gold" => "bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200",
        "Platinum" => "bg-violet-100 text-violet-800 dark:bg-violet-900 dark:text-violet-200",
        _ => ""
    };

    private record DeductibleOption(int Amount, int Premium);
    private record Coverage(string Name, bool Included, string? Limit = null);
    private record Product(string Name, string Tier, string Description, List<DeductibleOption> Deductibles, List<Coverage> Coverages, string MaxCoverage);

    private static readonly Product _product = new(
        "Health Plus Plan", "Gold",
        "Comprehensive coverage for individuals and families with low out-of-pocket costs.",
        new()
        {
            new(500, 450),
            new(1000, 350),
            new(2500, 250),
        },
        new()
        {
            new("Doctor visits", true, "Unlimited"),
            new("Hospital stays", true, "100% after deductible"),
            new("Prescription drugs", true, "$5k/year"),
            new("Mental health", true),
            new("Dental & Vision", false),
            new("International coverage", false),
        },
        "$2,000,000"
    );
}
