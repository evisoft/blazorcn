@* ============================================================
   Block: profile-mentor-availability
   Title: ProfileMentorAvailability
   Description: React mentor booking profile block for Next.js with calendar availability grid, session types, expertise tags, mentee reviews, and booking form using shadcn/ui, Tailwind CSS, and framer-motion
   Source: original/blocks/profile/profile-mentor-availability.tsx
   ============================================================ *@

<section class="mx-auto w-full max-w-2xl p-4">
    <div class="overflow-hidden rounded-lg border bg-card">

        @* Header *@
        <div class="flex items-start gap-4 border-b px-4 py-4">
            <AvatarCn Class="size-14">
                <AvatarFallbackCn Class="text-lg">RF</AvatarFallbackCn>
            </AvatarCn>
            <div class="min-w-0 flex-1">
                <span class="font-medium text-sm">Dr. Rachel Foster</span>
                <div class="mt-0.5 text-muted-foreground text-xs">Engineering Director at Datadog</div>
                <p class="mt-1.5 text-muted-foreground text-sm leading-relaxed">15 years in engineering leadership. Helping ICs transition to management and senior engineers reach staff level. 400+ mentees across 12 countries.</p>
            </div>
        </div>

        @* Stats *@
        <div class="flex items-center gap-4 border-b px-4 py-3">
            <span class="flex items-center gap-1 text-xs">
                <LucideIconCn Name="star" Size="12" Class="size-3 text-amber-500" />
                <span class="font-medium tabular-nums">4.9</span>
                <span class="text-muted-foreground">(186)</span>
            </span>
            <span class="text-muted-foreground text-xs">&middot;</span>
            <span class="flex items-center gap-1.5 text-xs">
                <LucideIconCn Name="users" Size="12" Class="size-3 text-muted-foreground" />
                <span class="font-medium tabular-nums">412</span>
                <span class="text-muted-foreground">sessions</span>
            </span>
            <span class="text-muted-foreground text-xs">&middot;</span>
            <span class="flex items-center gap-1.5 text-xs">
                <LucideIconCn Name="clock" Size="12" Class="size-3 text-muted-foreground" />
                <span class="text-muted-foreground">&lt; 2 hours response</span>
            </span>
        </div>

        @* Expertise *@
        <div class="border-b px-4 py-3">
            <span class="mb-2 block font-medium text-muted-foreground text-xs">Expertise</span>
            <div class="flex flex-wrap gap-1.5">
                @foreach (var topic in _expertise)
                {
                    <BadgeCn Variant="BadgeVariant.Secondary" Class="font-normal text-xs">@topic</BadgeCn>
                }
            </div>
        </div>

        @* Session types *@
        <div class="border-b px-4 py-3">
            <span class="mb-2.5 block font-medium text-muted-foreground text-xs">Session types</span>
            <div class="space-y-2">
                @foreach (var session in _sessionTypes)
                {
                    var sess = session;
                    var isSelected = _selectedSession == sess.Id;
                    <div role="button"
                         tabindex="0"
                         @onclick="@(() => _selectedSession = sess.Id)"
                         class="@Cn.Merge("cursor-pointer rounded-md border p-3 transition-colors", isSelected ? "border-foreground/20 bg-muted/50" : "hover:bg-muted/30")">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center gap-2">
                                <LucideIconCn Name="@GetSessionIcon(sess.Icon)" Size="14" Class="size-3.5 text-muted-foreground" />
                                <span class="font-medium text-sm">@sess.Name</span>
                            </div>
                            <div class="flex items-center gap-2 text-xs">
                                <span class="text-muted-foreground">@sess.Duration</span>
                                <span class="font-medium tabular-nums">@sess.Price</span>
                            </div>
                        </div>
                        <p class="mt-1 text-muted-foreground text-xs">@sess.Description</p>
                    </div>
                }
            </div>
        </div>

        @* Weekly availability grid *@
        <div class="border-b px-4 py-3">
            <div class="mb-2.5 flex items-center justify-between">
                <span class="font-medium text-muted-foreground text-xs">
                    <LucideIconCn Name="calendar" Size="12" Class="mr-1 inline size-3" />
                    This week
                </span>
                <span class="text-muted-foreground text-xs">PST timezone</span>
            </div>
            <div class="grid grid-cols-5 gap-1.5">
                @foreach (var daySchedule in _weekSchedule)
                {
                    var day = daySchedule;
                    <div class="space-y-1">
                        <span class="block text-center font-medium text-muted-foreground text-xs">@day.Day</span>
                        @foreach (var timeSlot in day.Slots)
                        {
                            var slot = timeSlot;
                            var slotKey = $"{day.Day}-{slot.Time}";
                            var slotIsSelected = _selectedSlot == slotKey;
                            <button type="button"
                                    disabled="@(!slot.Available)"
                                    @onclick="@(() => _selectedSlot = slotIsSelected ? null : slotKey)"
                                    class="@Cn.Merge("w-full rounded-md px-1 py-1.5 text-center text-xs transition-colors", !slot.Available ? "cursor-not-allowed bg-muted/30 text-muted-foreground/40" : slotIsSelected ? "bg-foreground text-background ring-2 ring-foreground ring-offset-1 ring-offset-card" : "bg-emerald-500/10 text-emerald-700 hover:bg-emerald-500/20 dark:text-emerald-400")">
                                @slot.Time
                            </button>
                        }
                    </div>
                }
            </div>
        </div>

        @* Reviews *@
        <div>
            <div class="border-b px-4 py-2">
                <span class="font-medium text-muted-foreground text-xs">Recent reviews</span>
            </div>
            @for (var i = 0; i < _reviews.Length; i++)
            {
                var review = _reviews[i];
                var idx = i;
                <div class="@Cn.Merge("px-4 py-3", idx < _reviews.Length - 1 ? "border-b" : "")">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center gap-2">
                            <span class="font-medium text-xs">@review.Name</span>
                            <span class="text-muted-foreground text-xs">@review.Role</span>
                        </div>
                        <div class="flex items-center gap-0.5">
                            @for (var s = 0; s < review.Rating; s++)
                            {
                                <LucideIconCn Name="star" Size="12" Class="size-3 fill-amber-500 text-amber-500" />
                            }
                        </div>
                    </div>
                    <p class="mt-1 text-muted-foreground text-sm leading-relaxed">@review.Text</p>
                    <span class="mt-1 block text-muted-foreground/60 text-xs">@review.Time</span>
                </div>
            }
        </div>

    </div>
</section>

@code {
    private record SessionType(string Id, string Name, string Duration, string Price, string Icon, string Description);
    private record TimeSlot(string Time, bool Available);
    private record DaySchedule(string Day, TimeSlot[] Slots);
    private record Review(string Id, string Name, string Role, int Rating, string Text, string Time);

    private string? _selectedSlot = null;
    private string? _selectedSession = "1";

    private static readonly string[] _expertise =
    [
        "Engineering Management",
        "System Design",
        "Career Growth",
        "Staff+ Engineering",
        "Technical Strategy",
        "Team Building",
    ];

    private static readonly SessionType[] _sessionTypes =
    [
        new("1", "1:1 Coaching", "45 min", "$120", "video", "Video call with screen sharing for career planning and technical mentorship"),
        new("2", "Resume Review", "30 min", "$75", "chat", "Async review with detailed written feedback within 48 hours"),
        new("3", "Mock Interview", "60 min", "$150", "video", "Simulated system design or behavioral interview with debrief"),
        new("4", "Quick Check-in", "15 min", "$40", "phone", "Brief call for quick questions or follow-up on previous sessions"),
    ];

    private static readonly DaySchedule[] _weekSchedule =
    [
        new("Mon", [new("9am", true),  new("12pm", false), new("3pm", true),  new("6pm", false)]),
        new("Tue", [new("9am", false), new("12pm", true),  new("3pm", true),  new("6pm", true)]),
        new("Wed", [new("9am", true),  new("12pm", true),  new("3pm", false), new("6pm", false)]),
        new("Thu", [new("9am", false), new("12pm", false), new("3pm", true),  new("6pm", true)]),
        new("Fri", [new("9am", true),  new("12pm", true),  new("3pm", true),  new("6pm", false)]),
    ];

    private static readonly Review[] _reviews =
    [
        new("1", "Alex P.", "Senior Engineer", 5, "Rachel helped me navigate my promotion to Staff. Her framework for building influence was exactly what I needed.", "1 week ago"),
        new("2", "Kim L.", "Engineering Manager", 5, "The mock system design interview was incredibly realistic. Got the offer at my target company.", "2 weeks ago"),
        new("3", "Raj S.", "Tech Lead", 4, "Great advice on building a technical strategy document. Would have liked more time for follow-up questions.", "3 weeks ago"),
    ];

    private static string GetSessionIcon(string icon) => icon switch
    {
        "video" => "video",
        "phone" => "phone",
        "chat" => "message-square",
        _ => "video",
    };
}
